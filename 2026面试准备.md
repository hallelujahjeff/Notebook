# 2026 面试准备

---

## Lua

### 闭包与 Upvalue

---

#### 核心概念

| 术语 | 定义 |
|---|---|
| **自由变量** | 函数内使用，但既非参数、也非内部局部变量的变量 |
| **闭包** | 函数 + 其捕获的自由变量引用环境的组合体 |
| **Upvalue** | Lua 对"被闭包捕获的自由变量"的具体实现机制 |

> Lua 中**所有函数都是闭包**（C 函数除外），即便没有捕获任何变量，只是 upvalue 列表为空。

---

#### Upvalue 的两种状态

##### Open（开放）

变量**仍在栈上**时，upvalue 是一个指向栈 slot 的**指针**，不持有值本身。

- 同一作用域内多个闭包捕获同一变量 → 共享同一个 UpVal 对象
- 修改对所有闭包立即可见

```
栈（Stack）
┌──────────────┐
│ slot[n]:  x  │ ◄── upvalue.v（指针）
└──────────────┘
```

##### Closed（关闭）

变量**离开作用域（出栈）时**，Lua 执行 close 操作：

1. 将值从栈复制到 UpVal 自身的堆内存
2. 将指针从指向栈改为指向自身内部存储
3. 外部访问方式不变（统一通过 `*v` 读写）

```
close 前：upvalue.v ──► stack[n]
close 后：upvalue.v ──► upvalue.u.value（堆上）
```

##### UpVal 数据结构（源码简化）

```c
typedef struct UpVal {
    TValue *v;          // 统一访问入口：open 时指向栈，closed 时指向 u.value
    union {
        TValue value;   // closed 后值存储在此
        struct {
            struct UpVal *prev;
            struct UpVal *next;
        } open;         // open 时构成链表，挂在 lua_State 上
    } u;
} UpVal;
```

---

#### 共享 Upvalue

```lua
local function test()
    local n = 0
    local function add() n = n + 1 end
    local function get() return n end
    return add, get
end

local add, get = test()
add(); add()
print(get())  -- 输出 2
```

`add` 和 `get` 共享**同一个** UpVal 对象，`test()` 返回后 `n` 被 close 到堆，两个闭包仍共享同一份值。

---

#### 经典陷阱：for 循环

##### 数值 for —— 每次迭代变量独立

```lua
local funcs = {}
for i = 1, 3 do
    funcs[i] = function() return i end
end
print(funcs[1](), funcs[2](), funcs[3]())
-- 输出：1  2  3
-- 原因：数值 for 每次迭代 i 是全新的局部变量，各闭包捕获独立 upvalue
```

##### 手动 while —— 共享同一变量

```lua
local funcs = {}
local i = 1
while i <= 3 do
    funcs[i] = function() return i end
    i = i + 1
end
print(funcs[1](), funcs[2](), funcs[3]())
-- 输出：4  4  4
-- 原因：三个闭包捕获同一个 i，循环结束时 i = 4
```

##### 修复：引入局部中间变量

```lua
local funcs = {}
local i = 1
while i <= 3 do
    local j = i                          -- 每次迭代创建新的局部变量
    funcs[i] = function() return j end
    i = i + 1
end
print(funcs[1](), funcs[2](), funcs[3]())
-- 输出：1  2  3
```

---

#### Upvalue 与 GC

- 闭包对 upvalue 持**强引用**，upvalue closed 后对其存储的值也持强引用
- 只要闭包可达，upvalue 引用的对象**不会被 GC**

**典型内存泄漏：**

```lua
local cache = {}
local function register(key)
    local huge_data = load_big_resource()
    cache[key] = function()
        return huge_data    -- 闭包持有 huge_data，导致无法被 GC
    end
end
-- 解法：用完后 cache[key] = nil，或用弱值表存储闭包
```

**循环引用：**

```lua
local a, b
a = function() return b end
b = function() return a end
-- Lua 使用标记-清除 GC，能正确处理循环引用，不会泄漏
```

---

#### 性能特性

访问速度：**局部变量 ≈ Upvalue > 全局变量（哈希查找）**

```lua
-- 热循环中将全局函数缓存为 upvalue，有实际性能收益
local sin = math.sin
local floor = math.floor
for i = 1, 1000000 do
    local v = sin(i) + floor(i)
end
```

---

#### 要点速览

| 维度 | 结论 |
|---|---|
| 本质 | 对自由变量的间接引用，不是值的副本 |
| Open 状态 | 变量在栈上，upvalue 是栈指针，多闭包共享同一对象 |
| Closed 状态 | 变量出栈，值迁移到堆，指针重定向，对外透明 |
| 共享机制 | 同作用域多个闭包捕获同一变量 → 共享同一 UpVal 对象 |
| for 循环 | 数值 for 每次迭代变量独立；手动循环外部变量共享 |
| GC 交互 | 闭包强引用 upvalue；标记清除可处理循环引用 |
| 性能 | upvalue 访问接近局部变量，远快于全局变量 |